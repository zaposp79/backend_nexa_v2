"""modules.calculator — HTTP API and persistence for the simulation calculator.

Sub-packages:
  api/        — FastAPI routes, handlers, DTOs (was calculator_api/).
  persistence/ — ResultsRepository, TraceabilityRepository (was calculator_persistence/).

The computation engine lives in modules.calculator_motor.
"""
